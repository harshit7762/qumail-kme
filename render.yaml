services:
  - type: web
    name: qumail-kme
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn kme_server:app --host 0.0.0.0 --port 10000"
